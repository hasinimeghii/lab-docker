# List all Docker images to identify the ones created during the TP
docker images

# Remove the locally created images
docker rmi back-image
docker rmi hasinimeghii/back-image

# Alternatively, remove all unused images at once
# docker image prune -a
